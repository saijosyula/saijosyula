# Claude Instructions

## Git Workflow
- After completing work and pushing PRs, use `git push --force origin main` to clean up the commit history on main (removes merge commits, keeps history clean).
